<Window x:Class="MarkHeath.MidiUtils.AdvancedOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Advanced Options" Height="260" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <CheckBox x:Name="CheckBoxRemoveSequencerSpecific" Content="Remove Sequencer Specific Events" Margin="0,4" />
        <CheckBox x:Name="CheckBoxRecreateEndTrack" Grid.Row="1" Content="Recreate End Track Events" Margin="0,4" />
        <CheckBox x:Name="CheckBoxAddNameMarker" Grid.Row="2" Content="Add Name Marker" Margin="0,4" />
        <CheckBox x:Name="CheckBoxTrimTextEvents" Grid.Row="3" Content="Trim Text Events" Margin="0,4" />
        <CheckBox x:Name="CheckBoxRemoveEmptyTracks" Grid.Row="4" Content="Remove Empty Tracks" Margin="0,4" />
        <CheckBox x:Name="CheckBoxRemoveExtraTempoEvents" Grid.Row="5" Content="Remove Extra Tempo Events" Margin="0,4" />
        <CheckBox x:Name="CheckBoxRemoveExtraMarkers" Grid.Row="6" Content="Remove Extra Markers" Margin="0,4" />
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" IsDefault="True" Width="80" Margin="0,0,8,0" Click="Ok_Click" />
            <Button Content="Cancel" IsCancel="True" Width="80" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
