<Window x:Class="AudioFileInspector.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options" Height="200" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,16">
            Audio File Inspector can be set up to appear as a right-click context menu option in Windows explorer. You can recreate or remove the file associations at any time using the buttons below.
        </TextBlock>
        <Button Grid.Row="1" Content="Recreate File Associations" Click="ButtonAssociate_Click" Margin="0,0,0,8" HorizontalAlignment="Left" Padding="12,4" />
        <Button Grid.Row="2" Content="Remove File Associations" Click="ButtonDisassociate_Click" HorizontalAlignment="Left" Padding="12,4" />
    </Grid>
</Window>
