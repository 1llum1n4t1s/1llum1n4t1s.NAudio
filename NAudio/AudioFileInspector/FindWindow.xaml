<Window x:Class="AudioFileInspector.FindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find" Height="120" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Find what:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="TextBoxFind" Grid.Column="1" Padding="4,2" KeyDown="TextBoxFind_KeyDown" />
        </Grid>
        <Button Grid.Row="1" Content="Find Next" IsDefault="True" Click="ButtonFind_Click" HorizontalAlignment="Right" Padding="16,4" />
    </Grid>
</Window>
