<Window x:Class="AudioFileInspector.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Audio File Inspector" Height="220" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="LabelProductName" Grid.Row="0" FontWeight="Bold" FontSize="14" Margin="0,0,0,4" />
        <TextBlock x:Name="LabelVersion" Grid.Row="1" Margin="0,0,0,4" />
        <TextBlock x:Name="LabelCopyright" Grid.Row="2" Margin="0,0,0,12" />
        <Button Grid.Row="4" Content="OK" IsDefault="True" IsCancel="True" Width="80" HorizontalAlignment="Right" Margin="0,12,0,0" Click="Ok_Click" />
    </Grid>
</Window>
