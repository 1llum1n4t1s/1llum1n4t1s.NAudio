<Window x:Class="MarkHeath.AudioUtils.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Output device:" Margin="0,0,0,4" />
        <ComboBox x:Name="ComboOutputDevice" Grid.Row="1" Margin="0,0,0,12" DisplayMemberPath="DeviceName" />
        <TextBlock Grid.Row="2" Text="Skip back (seconds):" Margin="0,0,0,4" />
        <TextBox x:Name="TextBoxSkipBackSeconds" Grid.Row="3" Margin="0,0,0,8" />
        <CheckBox x:Name="CheckUseAllSlots" Grid.Row="4" Content="Use all slots when shuffling" VerticalAlignment="Top" Margin="0,8,0,0" />
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" IsDefault="True" Click="Ok_Click" Width="80" Margin="0,0,8,0" />
            <Button Content="Cancel" IsCancel="True" Click="Cancel_Click" Width="80" />
        </StackPanel>
    </Grid>
</Window>
